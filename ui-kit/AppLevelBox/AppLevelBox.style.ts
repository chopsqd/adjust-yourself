import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
   bar: {
      position: "relative",
      borderRadius: 10,
      textAlign: "center",
      padding: 10,
      borderWidth: 2,
      borderColor: "#DEDEDE",
      marginTop: 10
   },
   title: {
      textAlign: "center"
   },
   lock: {
      position: "absolute",
      top: "50%",
      left: "50%"
   }
})
