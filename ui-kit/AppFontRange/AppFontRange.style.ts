import { StyleSheet } from "react-native"

export const styles = StyleSheet.create({
   range: {
      width: 200
   },
   container: {
      borderStyle: "solid",
      borderColor: "#DEDEDE",
      borderWidth: 2,
      borderRadius: 10,
      height: 100,
      padding: 5
   }
})
