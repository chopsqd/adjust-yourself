describe('getData method:', () => {
    test('should read data from AsyncStorage', () => {

    })

    test('should throw an error', () => {

    })
});

describe('setData method:', () => {
    test('should save data to AsyncStorage', () => {

    })

    test('should throw an error', () => {

    })
});
